
spring:
  jpa:
    database-platform: org.hibernate.dialect.SQLServer2012Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost;databaseName=todone;sendStringParametersAsUnicode=false
    username: programmer
    password: test
